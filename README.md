*Модуль 3. TypeScript (Задание #1)*Переведите проект на TypeScript

1. - Установил TS - loader и пакет для работы TypeScript
     npm install --save-dev typescript ts-loader

- Добавил файл tsconfig.json с настройками из документации webpack с добавлением rootDir": "./src" и отредактировал webpack.config.common.js повторно просматривая лекцию по webpack, т.к. в памяти ничего из этой лекции не осталось((

2. В файле index.ts исправил высвечивающиеся TypeScript-ом ошибки (буквально три строчки поправил), и изменил немного tsconfig.json, т.к. при build-е вылезали ошибки (погуглил их и добавил исключения exclude). Хотя не понял зачем это нужно было, если я указал  rootDir": "./src", в которой не было этих файлов. Но, после этих исправлений всё (почему-то, на моё удивление) заработало

